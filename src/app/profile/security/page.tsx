import ProfileLayout from "@/app/settings/profile-layout"
import SecuritySettings from "@/app/settings/security-settings"

export default function SecurityPage() {
  return (
    <ProfileLayout>
      <SecuritySettings />
    </ProfileLayout>
  )
}
